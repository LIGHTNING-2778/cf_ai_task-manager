{
  "name": "ai-task-manager",
  "version": "1.0.0",
  "description": "AI-powered task manager built on Cloudflare",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "start": "npx wrangler dev"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.51.0",
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}